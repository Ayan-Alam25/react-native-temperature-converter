import { StyleSheet } from "react-native";

export const s = StyleSheet.create({
  footer: {
    color: "white",
    fontWeight: "300",
    paddingTop: 20,
    position: "absolute",
    marginTop: 480,
    opacity: 0.5,
  },
});